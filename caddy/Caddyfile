:8421 {
    # Ensure MCP clients send both required Accept types
    request_header Accept "application/json, text/event-stream"

    reverse_proxy mcp:8420

    log {
        output file /data/hivemem-access.log
        format json
    }
}
