services:
  hivemem:
    build: .
    ports:
      - "8421:8421"
    volumes:
      - hivemem_data:/data
    restart: unless-stopped

volumes:
  hivemem_data:
    name: hivemem_data
  hivemem_models:
    name: hivemem_models
