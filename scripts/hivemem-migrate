#!/bin/sh
set -eu

echo "HiveMem Java applies Flyway migrations automatically at application startup."
echo "There is no standalone migration command in the Java runtime image."
echo "Start the service with the correct JDBC settings to apply pending migrations."
exit 1
